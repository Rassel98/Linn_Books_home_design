import 'package:flutter/material.dart';

class TwdWidgets extends StatelessWidget {
  const TwdWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('i am from twd'),
    );
  }
}
